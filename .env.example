# Optional: where the Node trail server (Socket.IO) lives in production.
# Static game + bigboard on Vercel/Cloudflare Pages; server on Fly/Render/Railway.
# Leave unset for local dev (Vite proxies /socket.io) or single-machine deploys.
# VITE_TRAIL_SERVER_URL=https://your-trail-server.example.com
